import 'package:flutter/material.dart';

class SnapchatProfile extends StatelessWidget {
  const SnapchatProfile({super.key});

  @override 
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}